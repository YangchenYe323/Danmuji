//! This crate provides the DanmuMonitor struct,
//! which drives a web-socket client to a given live room.
//! It holds a shared reference to a UserConfig(which saves the 
//! authorization information of a logged in user) and a RoomConfig(
//! saves the roomId to connect to).

pub struct DanmuMonitor {

}